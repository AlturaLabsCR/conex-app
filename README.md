# Conex Editor

## TODO

- [ ] Check if current behavior for deleting a site in one client and then modifying it on another is ok.
- [ ] When editor editing a site and then that site gets deleted in the sites tab, going back to the editor tab still shows the deleted site. Editor should reload since current behavior is to load the first site available or show a message that indicates to create a site when none are available.
- [ ] Add in the notice when logging in: Email sent to ... and the email address, plus a change email and resend buttons, although those buttons can be just clickable text not a full button.
- [ ] Allow adding links in the editor, by setting the name of the link and the url, just underline the links don't show them highlighted or different color or anything, and of course allow clicking on them and whatnot, probably add a small arrow icon on top right to indicate external link.
