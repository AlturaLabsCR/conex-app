# Conex Editor

## TODO

- [ ] Bug: After failed to authenticate, returns to login screen, but site list is never re-pulled after successful login.
- [ ] Feature: Add little sync icon in secondary color next to the Sites header to pull new sites.
- [ ] Feature: take a look at the API usage for tags, and do the following, show a grey pill with + icon, allow to write the tag and then show a check icon that when pressed patches with new tags.
- [ ] Feature: Delete sites by sliding them to left, show confirmation of course.
