# Conex Editor

## Roadmap

- [ ] Make the cards from account similar to the ones in sites, like, the grey
      secondary text should be the same size, as well as the card headings,
      so, copy formatting of the sites cards to the account cards.
- [ ] Make the floating editor toolbar appear on top of the keyboard when the
      keyboard is shown.
- [ ] Remove the scrollbar of the toolbar on mobile (keep scroll functionality).
- [ ] Make the URL in the site card a different container, right now it's in
      the same container as the heading for the site name, but this makes it
      wrap prematurely with the visibility pill.
- [ ] The editor spinner when loading the editor is too small and blue, I want
      the same text color and a bit larger, not huge but right now it's too
      small.
- [ ] For some reason, buttons like verify or login cannot be pressed when
      keyboard focused, which is odd.
- [ ] Add support for embedding images as base64 in the html of the editor,
      I'm not sure how they should be deleted, maybe a small `x` on top right
      corner of the image when tapped or hovered?
- [ ] Give the cards a slight background, like the border but not as much, just
      a very subtle background.
